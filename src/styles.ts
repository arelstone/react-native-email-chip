import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        flexDirection: 'row',
        flexWrap: 'wrap',
        backgroundColor: 'white',
        paddingVertical: 10,
    },
    inputContainer: {},
    input: {
        fontSize: 16,
        paddingHorizontal: 5,
        paddingVertical: 10,
        minWidth: 150,
    },
});
